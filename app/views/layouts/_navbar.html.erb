<nav class="navbar navbar-fixed-top navbar-top" role="navigation">
<!-- 
  <% if :goals == active.to_sym %>

    <div class="goal-list col-xs-1 text-center">
      <div id="wrapper">


          <div class="goal-icon" id="sidebar-wrapper">
              <ul class="sidebar-nav">
                  <li class="sidebar-brand"><a id="tree-toggle" href="#"><i class="fa fa-trophy fa-2x"></i></a>
                  </li>
              </ul>
          </div>


          <div id="tree">
              <div class="tree-element">
              	<% @goals.each do |goal| %>
                  	<h5><%= link_to goal.description, patient_goal_path(@patient.id, goal.id) %></h5>

                    <hr/>
                  <% end %> 
              </div>

              <%= link_to 'Add New Goal', new_patient_goal_path(@patient.id), :class=>"btn btn-green" %>
          </div>


        </div>
          
          <script>

  	        $("#tree-toggle").click(function(e) {
  	            e.preventDefault();
  	            $("#tree").toggleClass("active");
  	        });

          </script>
    </div>

  <% else %> 

    <div class="col-xs-1"></div>

  <% end %>
 -->
  <div id="navcollapse" class="collapse navbar-collapse">

    <div class="col-xs-12 text-right">
      <% if @patient.present? %>
        <% if @patient.id.present? %>
          <span><%= link_to @patient.name, patient_path(@patient.id), :class => "nav-link" %></span> 
        <% end %> 
      <% end %>
      <span><%= link_to "Log Out", destroy_user_session_path, method: :delete, :class => "nav-link" %></span>
    </div>

  </div>

</nav>



